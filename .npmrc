# @tensorflow-models/speech-commands (0.5.4, de 2021) declara como peer
# @tensorflow/tfjs-core ^3.0.0, pero funciona con tfjs 4.x en runtime.
# Sin esto, "npm install" falla con ERESOLVE (localmente, en Vercel y en CI).
legacy-peer-deps=true
